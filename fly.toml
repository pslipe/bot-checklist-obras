app = 'bot-checklist-obras'
primary_region = 'gru'

[build]
  dockerfile = "Dockerfile"

[processes]
  app = 'python bot.py'

[mounts]
  source = "checklist_data"
  destination = "/data"

[[vm]]
  memory = '256mb'
  cpus = 1